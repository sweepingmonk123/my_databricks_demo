# The main pipeline for zhangli_pipeline

resources:
  pipelines:
    zhangli_pipeline_etl:
      name: zhangli_pipeline_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/zhangli_pipeline_etl"

      libraries:
        - glob:
            include: ../src/zhangli_pipeline_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
